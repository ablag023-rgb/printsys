# Версии зафиксированы по фактически проверенному окружению: именно на этом
# составе проходят тесты и живой прогон печати.

# --- Обмен с сервером ---
httpx==0.28.1

# --- Документы: чтение, растеризация, резервный рендер ---
pypdf==6.12.1              # предпросмотр и нормализация страниц к A4
pypdfium2==5.8.0           # растеризация страниц для GDI
pillow==12.2.0             # ImageWin.Dib — отрисовка на контексте принтера
reportlab==4.5.1           # титул, заглушки, рендер xlsx без Excel
python-calamine==0.8.2     # основной ридер xlsx
openpyxl==3.1.5            # резервный ридер xlsx

# --- Окно оператора: веб-интерфейс сервера в WebView2 ---
# Второго набора шаблонов не заводим: клиент показывает ту же страницу, что
# и браузер. WebView2 есть в Windows 10/11 из коробки, ставить нечего.
pywebview==6.2.1
pythonnet==3.1.0          # мост pywebview к EdgeChromium

# --- Windows: печать, Excel COM, хранение токена ---
pywin32==311; sys_platform == "win32"
keyring==25.7.0; sys_platform == "win32"

# --- Тесты ---
pytest==9.0.3

# --- Только для АВТОНОМНОЙ тестовой сборки: сервер внутри клиента ---
# Обычной сборке не нужны; ставятся, потому что PyInstaller собирает все
# варианты одним проходом.
fastapi==0.115.6
uvicorn==0.34.0
sqlalchemy==2.0.36
aiosqlite==0.20.0
jinja2==3.1.4
apscheduler==3.11.0
argon2-cffi==23.1.0
python-jose==3.3.0
boto3==1.35.81
