"""Точка входа АВТОНОМНОЙ тестовой сборки.

Сервер поднимается внутри процесса, документы берутся из папки «Документы»
рядом с программой. Ни сети, ни внешних источников. Печать — настоящая.
"""
import sys

from printsys_client.webui import main

if __name__ == "__main__":
    sys.exit(main(standalone=True))
